#include <iostream>
using namespace std;

typedef long long ll;

int main(){
    int t;
    scanf("%d",&t);
    while(t--){
        int n;
        scanf("%d",&n);
        ll ns[n];
        for(int i=0;i<n;i++) scanf("%lld",&ns[i]);
        for(int i=1;i<n;i++){

        }
    }
    return 0;
}
