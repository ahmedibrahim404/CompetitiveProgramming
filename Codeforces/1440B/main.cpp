#include <iostream>

using namespace std;
typedef long long ll;
int t;

int main(){
    cin >> t;
    while(t--){
        int n, k;
        cin >> n >> k;
        ll ns[n*k];
        for(ll i=0;i<n*k;i++) cin >> ns[n*k-i-1];
        ll ans=0;
    }
    return 0;
}
