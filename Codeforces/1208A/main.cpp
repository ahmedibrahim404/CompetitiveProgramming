#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
int T, a, b, n;
vector<ll> ns(1e9+3);
int main(){
    for(int i=2;i<)
    scanf("%d",&T);
    while(T--){
        scanf("%d%d%d",&a,&b,&n);

    }
    return 0;
}
