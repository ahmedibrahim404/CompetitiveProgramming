#include <iostream>

using namespace std;
int T,N,A[10000+3],R[10000+3];
int main(){
    scanf("%d",&T);
    while(T--){
        scanf("%d",&N);
        for(int i=0;i<N;i++)scanf("%d",&A[i]);
        for(int i=0;i<N;i++)scanf("%d",&R[i]);
        for(int i=0;i<N;i++){
            for(int j=0;j<R[i];j++){

                int idx=i;
            }
        }
    }
    return 0;
}
