#include <iostream>

using namespace std;

// N^2 for dense graphs ( edges ~ N^2 )
/**
    idea is simply you start with any node
    add the nearest node to it and update the distance of all nodes to them ( w.r.t. new added node )
    and get back to previous step
**/

int main(){
    return 0;
}
