#include <iostream>

using namespace std;

/** Overlap-Friendly functions are those for which F( F(a, b), F(b,c )) = F(a, F(b, c))
    max, min, gcd, constant are
    but +, -, * aren't
**/
int main(){
    return 0;
}
